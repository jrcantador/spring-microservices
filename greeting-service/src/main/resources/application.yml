server:
  port: 8080
spring:
 application:
  name: gretting-service
 config:
    import:
    - optional:configserver:http://localhost:8888/
 cloud:
    config:
      profile: qa
management:
 endpoints:
  web:
   exposure:
    include:
    - '*'
gretting-service:
 gretting: "olá"
 default-value: "Mundo"
